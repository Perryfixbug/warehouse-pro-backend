<div class="space-y-8">
  <!-- 🌟 Tiêu đề -->
  <div>
    <h1 class="text-3xl font-bold text-gray-800 mb-2">Thống kê tổng quan kho hàng</h1>
    <p class="text-gray-500">Tổng hợp dữ liệu nhập - xuất - tồn kho và nhân sự quản lý</p>
  </div>

  <!-- 🔢 Các chỉ số tổng quan -->
  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
    <div class="bg-white rounded-2xl shadow p-5 border border-gray-100">
      <div class="text-gray-500 text-sm">Tổng sản phẩm</div>
      <div class="text-3xl font-semibold text-blue-600 mt-1"><%= @product_count %></div>
    </div>

    <div class="bg-white rounded-2xl shadow p-5 border border-gray-100">
      <div class="text-gray-500 text-sm">Đơn nhập tháng này</div>
      <div class="text-3xl font-semibold text-green-600 mt-1"><%= @import_this_month %></div>
    </div>

    <div class="bg-white rounded-2xl shadow p-5 border border-gray-100">
      <div class="text-gray-500 text-sm">Đơn xuất tháng này</div>
      <div class="text-3xl font-semibold text-orange-500 mt-1"><%= @export_this_month %></div>
    </div>

    <div class="bg-white rounded-2xl shadow p-5 border border-gray-100">
      <div class="text-gray-500 text-sm">Tổng giá trị tồn kho</div>
      <div class="text-3xl font-semibold text-gray-700 mt-1"><%= number_to_currency(@inventory_value, unit: "₫", precision: 0) %></div>
    </div>
  </div>

  <!-- 📈 Biểu đồ -->
  <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
    <!-- Nhập hàng -->
    <div class="bg-white rounded-2xl shadow p-6 border border-gray-100">
      <h2 class="text-lg font-semibold text-gray-700 mb-4">📥 Biểu đồ nhập hàng (30 ngày gần nhất)</h2>
      <%= line_chart @import_chart_data, height: "250px", colors: ["#22c55e"], xtitle: "Ngày", ytitle: "Số đơn nhập" %>
    </div>

    <!-- Xuất hàng -->
    <div class="bg-white rounded-2xl shadow p-6 border border-gray-100">
      <h2 class="text-lg font-semibold text-gray-700 mb-4">📤 Biểu đồ xuất hàng (30 ngày gần nhất)</h2>
      <%= line_chart @export_chart_data, height: "250px", colors: ["#f97316"], xtitle: "Ngày", ytitle: "Số đơn xuất" %>
    </div>
  </div>

  <!-- ⚠️ Cảnh báo hàng sắp hết -->
  <div class="bg-white rounded-2xl shadow p-6 border border-gray-100">
    <h2 class="text-lg font-semibold text-gray-700 mb-4 flex items-center">
      <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 mr-2 text-red-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M12 2a10 10 0 1010 10A10 10 0 0012 2z" />
      </svg>
      Sản phẩm sắp hết hàng
    </h2>

    <table class="w-full text-left border-t border-gray-100">
      <thead class="text-gray-500 text-sm uppercase">
        <tr>
          <th class="py-2">Tên sản phẩm</th>
          <th class="py-2">Số lượng còn</th>
          <th class="py-2">Ngưỡng cảnh báo</th>
          <th class="py-2">Trạng thái</th>
        </tr>
      </thead>
      <tbody class="divide-y divide-gray-100">
        <% @low_stock_products.each do |product| %>
          <% status_class, status_text =
              if product.quantity <= 0
                ["text-red-600", "Hết hàng"]
              elsif product.quantity < 10
                ["text-yellow-600", "Cần nhập thêm"]
              else
                ["text-green-600", "Đủ hàng"]
              end %>

          <tr>
            <td class="py-2"><%= product.name %></td>
            <td class="py-2 <%= status_class %> font-semibold"><%= product.quantity %></td>
            <td class="py-2"><%= 10 %></td>
            <td class="py-2 <%= status_class %> font-medium"><%= status_text %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <% if @low_stock_products.empty? %>
      <p class="text-center text-gray-500 italic py-4">✅ Tất cả sản phẩm đều đủ hàng.</p>
    <% end %>
  </div>
</div>
